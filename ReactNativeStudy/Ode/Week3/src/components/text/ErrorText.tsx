import React from 'react';
import {Text} from 'react-native';
import Colors from '../../styles/colors';

export default function ErrorText() {
  return <Text style={{color: Colors.White}}>문제가 발생했습니다.</Text>;
}
