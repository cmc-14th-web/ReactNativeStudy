import React from 'react';
import {Text} from 'react-native';
import Colors from '../../styles/colors';

export default function LoadingMoreText() {
  return <Text style={{color: Colors.White}}>더 불러오는 중...</Text>;
}
