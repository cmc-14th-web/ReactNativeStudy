import React from 'react';
import {Text} from 'react-native';
import Colors from '../../styles/colors';

export default function EmptyText() {
  return <Text style={{color: Colors.White}}>비었어유</Text>;
}
